# Python dependencies for the sync scripts (scripts/sync-cost.py,
# scripts/sync-bios.py). Pinned by major version so security patches
# still apply automatically and the GitHub Actions cache key stays
# stable.
requests>=2.34.2,<3
beautifulsoup4>=4.15.0,<5
# Pillow is used by sync-bios.py to downscale + re-encode headshots
# uploaded via the Google Form. Optional at runtime — sync-bios.py
# falls back to copying the raw bytes if Pillow is unavailable.
Pillow>=12.3.0,<13
