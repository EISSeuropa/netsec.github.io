[pytest]
# The script test suites are named test-<script>.py (hyphen) to mirror
# their hyphenated targets, which pytest's default `test_*.py` glob does
# not collect. Add the hyphen pattern and point collection at scripts/.
python_files = test_*.py test-*.py
testpaths = scripts
